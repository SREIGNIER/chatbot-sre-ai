import streamlit as st
from chatbot import ask_question
from loader import load_documents
from vectorizer import create_vectorstore

st.set_page_config(page_title="SRE AI", layout="wide")
st.title("🤖 SRE AI - Chatbot interne")

user_input = st.text_input("Pose ta question ici")

if "vectorstore" not in st.session_state:
    docs = load_documents("docs")
    st.session_state.vectorstore = create_vectorstore(docs)

if user_input:
    response = ask_question(user_input, st.session_state.vectorstore)
    st.write(f"**Réponse :** {response}")
